# Kubernetes 调度器可视化沙箱功能说明文档

## 1. 项目概述

本项目是一个基于 React、Tailwind CSS 和 Framer Motion 开发的 Kubernetes 调度器可视化沙箱 Demo。它用于模拟真实集群中的 Pod 持续提交、队列堆积、调度决策、节点绑定、资源占用变化和调度失败挂起等流程。

项目核心目标是构建一个“事件驱动、实时流式、算法可插拔”的前端沙箱：

- 通过实时任务流模拟用户持续提交 Pod。
- 使用标准 React State 数组维护 Pending Queue。
- 将调度算法抽离为独立纯函数，方便未来替换为真实后端调度器 API。
- 使用 Framer Motion 展示从调度中心飞入目标节点的动态绑定过程。
- 提供终端日志，持续输出调度事件流。
- 支持切换到真实 K8s 模式，通过后端代理接入 Kubernetes API、调度决策和事件流。

## 2. 技术栈

- React：负责组件化 UI 与状态驱动渲染。
- TypeScript：提供任务、节点、日志、调度状态等类型约束。
- Tailwind CSS：负责大屏监控风格布局与视觉样式。
- Framer Motion：负责队列卡片进出、Broker 决策态、任务飞行动画和资源条平滑过渡。
- Vite：负责本地开发与构建。
- Lucide React：提供控制台、节点、终端、按钮等图标。

## 3. 功能模块

### 3.1 顶部控制台

顶部控制台用于控制模拟流量和调度策略，包含以下功能：

- 运行模式切换：支持 `模拟沙箱` 和 `真实 K8s` 两种模式。
- 实时流量开关：开启后，系统按照当前频率持续生成新任务并追加到 Pending Queue。
- 流量速度滑块：控制任务生成频率，范围为 500ms 到 5000ms 一个任务。
- 调度策略切换：支持 `LeastRequested` 和 `MostRequested` 两种前端伪调度策略。
- 手动突发按钮：一次性向队列追加 10 个任务，用于模拟高并发突发流量。
- 队列和挂起数量展示：实时显示当前队列深度与失败挂起任务数量。

在真实 K8s 模式下：

- 实时流量由后端 watch Kubernetes Pending Pod 事件提供。
- 手动突发按钮会请求后端在受控 namespace 中创建测试 Pod。
- 页面会显示 K8s 事件流连接状态。

### 3.2 流式任务队列

左侧 `Live Queue` 组件展示等待调度的任务队列，符合 FIFO 语义：

- 新任务进入队尾。
- 调度器从队头取出任务。
- 队列卡片会随任务流入和弹出实时动画更新。
- 每个任务卡片展示任务名称、CPU 请求、内存请求、创建时间和等待耗时。
- 队头任务使用 `HEAD` 标识，便于观察当前即将被调度的 Pod。

### 3.3 中央动态调度看板

中央 `Live Broker` 展示当前正在被处理的任务：

- 空闲时显示等待队头任务。
- 取到任务后进入 `decision` 状态，展示“决策中...”动效。
- 决策完成后进入 `flying` 状态，显示目标节点和调度分值。
- 调度动画与任务流入互不阻塞，新任务仍可持续进入队列。

### 3.4 集群节点拓扑

右侧 `Node Topology Matrix` 展示模拟集群节点：

- 当前内置 4 个物理节点。
- 每个节点展示名称、可用区、角色、Pod 数量。
- 每个节点维护 CPU 和内存资源进度条。
- 资源条包含实时轻微波动，用于模拟监控指标的动态变化。
- 当任务绑定到节点后，CPU 和内存占用会平滑上升。
- 节点内部展示当前驻留的 Pod 标签。
- 点击 Pod 标签可模拟 Pod 销毁，并立即释放对应 CPU 和内存资源。

### 3.5 调度失败与等待重试区

当集群资源不足，无法承载当前任务时：

- 任务不会死锁在 Pending Queue 中。
- 调度器会将任务弹出队列。
- 任务进入 `Pending / Failed` 挂起区。
- 终端输出资源不足导致 Pending 的日志。
- 用户可手动重试入队，也可丢弃挂起任务。

### 3.6 实时日志终端

底部 `Real-time Terminal` 持续滚动输出事件日志，包括：

- 新任务流入。
- 调度策略选择。
- 调度成功绑定。
- 调度失败原因。
- Pod 销毁与资源释放。
- 挂起任务重试或丢弃。

日志示例：

```text
[12:30:01] 📥 实时收到新任务: pod-nginx-9k2 (CPU: 2, Mem: 4G)
[12:30:02] ⚡ 调度决策中... 选用 LeastRequested 策略
[12:30:02] 🚀 成功绑定: pod-nginx-9k2 -> node-compute-1 (分值: 92)
[12:30:05] ❌ 调度失败: pod-heavy-db 无法适配任何节点 (原因: 内存不足)
```

## 4. 架构设计

项目按“高内聚、低耦合”的方式划分为四层。

### 4.1 流量生成器：Task Ingestion Line

对应文件：

- `src/hooks/useTaskIngestion.ts`
- `src/data/tasks.ts`

职责：

- 使用 `setInterval` 按指定频率生成任务。
- 将新任务通过回调推送给上层状态。
- 不直接关心队列如何消费，也不关心 UI 如何渲染。

任务生成逻辑位于 `createTask` 和 `createTaskBurst` 中。每个任务包含：

- `id`
- `name`
- `reqCpu`
- `reqMem`
- `createdAt`
- `image`
- `priority`

### 4.2 缓冲队列：Pending Queue

对应文件：

- `src/App.tsx`

职责：

- 使用 React State 数组维护待调度任务。
- 新任务追加到数组尾部。
- 调度器从数组头部取任务。
- 队列流入和调度动画并行运行，避免动画阻塞新任务进入。

核心状态：

```ts
const [pendingQueue, setPendingQueue] = useState<Task[]>([]);
```

### 4.3 独立调度器钩子：Pluggable Scheduler Engine

对应文件：

- `src/hooks/useSchedulerEngine.ts`
- `src/scheduler/predictTargetNode.ts`
- `src/api/k8sApi.ts`

核心调度接口：

```ts
function predictTargetNode(
  task: Task,
  nodes: ClusterNode[],
  strategy: SchedulerStrategy,
): string | null
```

当前实现为前端伪逻辑：

- `LeastRequested`：优先选择任务放入后资源负载最低的节点。
- `MostRequested`：优先选择任务放入后资源负载最高但仍可承载的节点。
- 若所有节点都无法承载任务，则返回 `null`。

该函数是模拟模式下的调度决策边界。真实 K8s 模式下，前端会通过 `src/api/k8sApi.ts` 请求后端：

- `GET /api/k8s/snapshot`：同步真实节点和 Pending Pod 快照。
- `GET /api/k8s/events`：通过 SSE 接收真实集群事件流。
- `POST /api/k8s/scheduler/decisions`：请求真实调度决策。
- `POST /api/k8s/bindings`：提交真实 Kubernetes Binding。
- `POST /api/k8s/pods/delete`：删除真实 Pod。
- `POST /api/k8s/tasks/burst`：创建测试 Pod 突发流量。

UI 层不依赖算法细节，只依赖最终返回的 `nodeId | null`。

### 4.4 渲染与动效层：UI & Animation

对应文件：

- `src/components/TopConsole.tsx`
- `src/components/LiveQueue.tsx`
- `src/components/LiveBroker.tsx`
- `src/components/FlyingTaskLayer.tsx`
- `src/components/NodeTopologyMatrix.tsx`
- `src/components/FailedQueue.tsx`
- `src/components/TerminalLog.tsx`

职责：

- 根据状态变化渲染界面。
- 不直接实现调度算法。
- 不直接修改调度决策。
- 使用 Framer Motion 实现队列动画、飞行动画和资源条过渡。

## 5. 核心流程

### 5.1 实时任务流入流程

1. 用户开启实时流量。
2. `useTaskIngestion` 启动定时器。
3. 定时器调用 `createTask` 生成任务。
4. 新任务追加到 `pendingQueue` 队尾。
5. `LiveQueue` 根据状态变化实时展示新卡片。
6. `TerminalLog` 输出任务流入日志。

### 5.2 调度成功流程

1. 调度引擎检测到队列非空且当前没有任务飞行动画。
2. 从队头弹出一个任务。
3. Broker 进入“决策中”状态，展示 300ms 动效。
4. 调用 `predictTargetNode` 获取目标节点。
5. 创建飞行动画任务。
6. 任务卡片从 Broker 飞入目标节点。
7. 飞行动画完成后，将任务写入目标节点 `pods` 列表。
8. 目标节点 CPU 和内存使用量增加。
9. 资源条平滑过渡到新占用比例。
10. 终端输出成功绑定日志。

### 5.3 调度失败流程

1. 调度引擎从队头取出任务。
2. `predictTargetNode` 返回 `null`。
3. 系统计算失败原因，例如 CPU 不足、内存不足或资源碎片化。
4. 任务进入 `FailedQueue`。
5. 终端输出调度失败和 Pending 状态日志。
6. 用户可选择重试或丢弃该任务。

### 5.4 Pod 销毁流程

1. 用户点击某个节点内部的 Pod 标签。
2. 系统从该节点 `pods` 列表中移除该任务。
3. 节点释放该任务占用的 CPU 和内存。
4. 节点资源条平滑回落。
5. 终端输出 Pod 销毁日志。

## 6. 数据结构说明

核心类型位于 `src/types/scheduler.ts`。

### 6.1 Task

表示待调度任务：

```ts
interface Task {
  id: string;
  name: string;
  reqCpu: number;
  reqMem: number;
  createdAt: number;
  image: string;
  priority: 'low' | 'normal' | 'high';
}
```

### 6.2 ClusterNode

表示 Kubernetes 物理节点：

```ts
interface ClusterNode {
  id: string;
  name: string;
  zone: string;
  role: string;
  capacityCpu: number;
  capacityMem: number;
  usedCpu: number;
  usedMem: number;
  pods: ScheduledPod[];
}
```

### 6.3 BrokerState

表示中央调度看板状态：

```ts
interface BrokerState {
  phase: 'idle' | 'decision' | 'flying';
  task: Task | null;
  targetNodeId?: string;
  score?: number;
}
```

## 7. 运行方式

安装依赖：

```bash
npm install
```

启动开发服务：

```bash
npm run dev
```

构建生产包：

```bash
npm run build
```

本地访问地址通常为：

```text
http://localhost:5173/
```

真实 K8s 模式需要配置后端代理地址：

```bash
VITE_K8S_API_BASE_URL=/api/k8s
```

后端接口契约见：

```text
docs/k8s-backend-contract.md
```

后端代理实现位于：

```text
server/
```

后端本地命令：

```bash
cd server
npm install
npm run dev
npm run build
npm test
```

前端容器化部署文件：

```text
Dockerfile
nginx.conf
k8s/frontend.yaml
```

后端容器化和 K8s 部署文件：

```text
server/Dockerfile
k8s/rbac.yaml
k8s/scheduler-api.yaml
```

## 8. 后续扩展方向

### 8.1 接入真实后端调度算法

项目已新增真实 K8s API 适配层。后端需要实现：

- Kubernetes API watch。
- Pending Pod 入队。
- 调度决策。
- Binding API 调用。
- Pod 删除。
- SSE 事件推送。

后端返回建议包含：

- `targetNodeId`
- `score`
- `reason`
- `diagnostics`

### 8.2 支持更多调度策略

可扩展策略包括：

- `BalancedAllocation`
- `NodeAffinity`
- `TaintToleration`
- `PriorityPreemption`
- `BinPacking`
- `SpreadScheduling`

### 8.3 增加真实集群指标

未来可接入 Prometheus、Kubernetes Metrics Server 或自定义 WebSocket 流，将节点 CPU、内存和 Pod 状态替换为真实数据。

### 8.4 增加调度回放能力

可将日志事件保存为事件流，支持暂停、回放、倍速播放和故障复盘。

## 9. 当前实现特点

- 调度动画不会阻塞实时任务流入。
- Pending Queue 使用标准 React State 数组实现。
- 调度核心和 UI 层解耦。
- 节点资源会随任务绑定和销毁实时变化。
- 调度失败任务不会卡死队列。
- 所有关键事件都会写入底部实时终端。
